# Claude 할 일 — 문서공방(AIM 프로젝트관리)

> Claude(AI 어시스턴트)가 맡아서 진행하는 작업 목록. 사람 몫(이돈우·방현주)은 맨 아래 따로 표기.
> 갱신: 2026-06-19

---

## ✅ 완료 (오늘까지, 2026-06-19)

- 저장소를 프로젝트 **루트로 이동**(run.bat 등 루트에서 실행), 포트 7001 옛 v0.9.1 서버 정리 → `/`→`/studio` 정상.
- **codex/agy 로그인 수정**: 로그인 명령 절대경로화, 공백 경로 따옴표, 미설치 사전안내, setup.bat 폴백 검사.
- **회사 마스터 템플릿 배선**: `assets/hwpx_template.hwpx`(한글 마스터), `assets/pptx_template.pptx`(회사양식PPT). McKinsey는 자기 템플릿 유지.
- **레시피 128개 구성**: 기존 16(`001`~`016`) + 신규 111(`017`~`127`) + McKinsey(`999`). 3자리 번호 + 포맷표식(한글/엑셀/파워포인트/워드) + 카테고리 자동 분류.
- **버그1(긴 파일명/경로)** 수정, **버그2(입력 미첨부 에러)** → 모든 입력 `required=false`(비차단).
- **옵션 A(전역 환각 방지 프롬프트)** 주입 — 모든 생성에 "근거 기반 + [확인 필요]" 규율 강제.
- 문서화: `근거기반-생성_RAG-딥리서치-현황과-도입옵션.md`, `_context/AIMRAW/_RAG색인자료-위치안내.md`.

---

## ⏳ 예정 (확정 일정)

> 방침: "내일/모레" 순서는 임시였음. **실제로는 내일 RAG부터 만들고, 주말 안에 마무리**, 그 다음 문서 확정.
> RAG는 **지금 가진 AIMRAW 자료만으로** 먼저 구축(RD 미확보·대용량 미정리 상태 그대로, 핵심 파일 위주 PoC).

### 내일 (D+1) — RAG(옵션 B) 구축 + 검증
1. **인프라 켜기**: ChromaDB + 로컬 임베딩(fastembed, 오프라인). venv에 설치.
2. **색인**: AIMRAW 핵심 자료(M&E guideline·PDM·양식 등) → 텍스트 추출 → 임베딩 → 벡터DB(중복·대용량 제외).
3. **문서공방 연결**(옵션 B): 생성 시 관련 자료 자동 검색→근거 주입. RAG 꺼져도 자동 우회.
4. **검증**: `014-파워포인트-10차-파견-WrapUp-발표자료`로 생성 → PPTX 출력 + RAG 근거주입 동시 확인.
   - ⚠️ **선행**: 코드 변경(버그·프롬프트 A) 반영 위해 **run.bat 재시작**.

### 주말 내 — 마무리
- 한글(HWPX) 산출 확인(한글 마스터 적용), 색인 범위 확대(여력 되면 1차 아카이브), 안정화.
- **그 다음: 문서 확정**(레시피로 실제 산출물 생성·확인).

### 이후 — 기능 (여력 시)
- **"+ 입력 추가" 버튼**(프런트 `static/studio/studio.js`). 백엔드는 임의 필드 이미 허용.
- **PDF 출력**: 확장자 정책 정해지면 반영.

> RAG 상세 단계·색인 자료 위치: `_context/AIMRAW/_RAG색인자료-위치안내.md` 6장.

---

## 사람 몫 (참고)

- **이돈우**: 옵션 의사결정(RAG 셋업·PDF 확장자·코퍼스 범위), 프롬프트 정교화(산출물별), 함께 검증.
- **방현주**: 1차 자료 정리(초대형→TXT, 표 xlsx 원본유지, 중복·desktop.ini 제외), **RD(사업수행계획서) 확보**.
